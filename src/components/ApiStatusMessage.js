// In this i show the api status 
import React from 'react'

export default function ApiStatusMessage({ message }) {
  return (
    <div className="api-status-message">
    <p>{message}</p>
  </div>

  )
}
